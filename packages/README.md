This folder is for shared packages/libraries. Create packages here, for example `packages/ui` or `packages/utils`.

Each package should have its own `package.json` and expose a build/test pipeline so Turborepo can orchestrate it.
